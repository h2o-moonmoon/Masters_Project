name: exercise-form-ai
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pytorch=2.3
  - torchvision
  - pytorch-cuda=12.1  # remove if CPU only
  - opencv
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - tqdm
  - pip
  - pip:
      - ultralytics==8.2.0  # YOLOv8 incl. pose
      - albumentations
